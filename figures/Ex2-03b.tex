\documentclass[12pt,a4paper]{article}
\usepackage[T1]{fontenc}
\usepackage[pdftex,active,tightpage]{preview}

\setlength\PreviewBorder{1em} % use to add a border around the image

\usepackage{tikz}

\begin{document}
\begin{preview}
\begin{tikzpicture}
  \draw[help lines,gray!40] (0,0) circle (5);
  \pgfmathsetmacro{\a}{5*cos(200)}
  \pgfmathsetmacro{\b}{5*sin(200)}
  \filldraw (\a,\b) circle (2pt) node[below left] {$p_1 = (x_1,y_1)$};
  \pgfmathsetmacro{\c}{5*cos(20)}
  \pgfmathsetmacro{\d}{5*sin(20)}
  \draw[thick] (\a,\b) -- (\c,\d);
  \draw[help lines,gray!40,->] (-6,\b) -- (6,\b);
  \filldraw (\c,\d) circle (2pt) node[above right] {$p_2 = (x_2,y_2)$};
  \draw[thick] (\a,\b) + (0.8,0) arc (0:50:0.8);
  \draw[thick] (\a,\b) + (0.85,0) arc (0:50:0.85) ++ (0.1,-0.05)%
    node[right] {$a$};
  \begin{scope}[gray]
  \draw[thick] (\a,\b) + (1.9,0) arc (0:10:1.9);
  \draw[thick] (\a,\b) + (1.95,0) arc (0:10:1.95) ++ (0.1,-0.15)%
    node[right] {$a$};
  \draw[thick] (\a,\b) + (1.4,0) arc (0:-15:1.4);
  \draw[thick] (\a,\b) + (1.45,0) arc (0:-15:1.45) ++ (0.05,0.1)%
    node[right] {$a$};
  \end{scope}[gray]
  \begin{scope}[thick,dashed]
    \pgfmathsetmacro{\angle}{55}
    \pgfmathsetmacro{\e}{\a + 10*cos(\angle)*cos(20+\angle)}
    \pgfmathsetmacro{\f}{\b + 10*cos(\angle)*sin(20+\angle)}
    \pgfmathsetmacro{\g}{\c + 10*cos(\angle)*cos(-160+\angle)}
    \pgfmathsetmacro{\h}{\d + 10*cos(\angle)*sin(-160+\angle)}
    \draw[gray!50] (\a,\b) -- (\e,\f) -- (\c,\d) -- (\g,\h) -- (\a,\b);
    \pgfmathsetmacro{\angle}{80}
    \pgfmathsetmacro{\e}{\a + 10*cos(\angle)*cos(20+\angle)}
    \pgfmathsetmacro{\f}{\b + 10*cos(\angle)*sin(20+\angle)}
    \pgfmathsetmacro{\g}{\c + 10*cos(\angle)*cos(-160+\angle)}
    \pgfmathsetmacro{\h}{\d + 10*cos(\angle)*sin(-160+\angle)}
    \draw[gray!50] (\a,\b) -- (\e,\f) -- (\c,\d) -- (\g,\h) -- (\a,\b);
    \pgfmathsetmacro{\angle}{30}
    \pgfmathsetmacro{\e}{\a + 10*cos(\angle)*cos(20+\angle)}
    \pgfmathsetmacro{\f}{\b + 10*cos(\angle)*sin(20+\angle)}
    \pgfmathsetmacro{\g}{\c + 10*cos(\angle)*cos(-160+\angle)}
    \pgfmathsetmacro{\h}{\d + 10*cos(\angle)*sin(-160+\angle)}
    \draw (\a,\b) -- (\e,\f) -- (\c,\d) -- (\g,\h) -- (\a,\b);
  \end{scope}
\end{tikzpicture}
\end{preview}
\end{document}
